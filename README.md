# lunatechs-chapters

Chapter pages + event lists for lunatechs.social, vibe-coded by chapter owners.
Separate from the main site repo (`lunatechs-www`) so a chapter edit can never
touch the site's code, build, infra, or another chapter. See `CLAUDE.md`.

Build locally: `node scripts/build.mjs && node scripts/validate.mjs`
